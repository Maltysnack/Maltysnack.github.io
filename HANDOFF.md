# HANDOFF

Living scratchpad. Read first thing each session, update last thing. Overwrite freely; this is a snapshot, not a log.

## Current state (as of 08-05-2026)

- Site is in good shape and being linked in job applications.
- Tone bar in `CLAUDE.md` under "Context": personal but not edgy.
- Pages auto-listed in `README.md` from `<meta name="description">` per page. Run `python3 scripts/build-readme.py` after adding or renaming a sidebar page.
- Layout system: `data-layout="prose|page|wide|full"` on `<main>`. Don't hardcode max-widths.
- Favicons: `sidebar.js` injects them via `FAVICON_VERSION`. Bump there when icons change.
- Wren routine fires Wed + Sun ~09:00. Logs every run (ship or skip) to `/wren/_runlog.md`.
- Editorial sanitize routine fires Sun ~10:00. Quiet janitor sweep.
- Pre-commit hook lives at `scripts/hooks/pre-commit`. Activate per clone with `git config core.hooksPath scripts/hooks`.

## Active subsystem: `/dnd/` D&D character sheet engine

- 4 live characters: Grosh (Triton Paladin Ancients 5), Shieka (Giant Warlock Fiend 5), Jhotan (Forest Gnome Battle Master 5), Japlan (Standard Human Sorcerer Draconic Brass 5).
- Engine reads JSON per character, renders to a single-screen sheet with wiki-linked names everywhere.
- Upload flow: visitor drops a filled WotC PDF on `/dnd/`, Vercel function `api/upload.js` opens a PR. Wren polishes, maltysnack merges, build action regenerates `index.json` + per-character HTML shim.
- Update flow: live sheet has an Update Sheet block; freeform "what changed" + state, `api/update.js` opens an update PR. Same polish + merge loop.
- Two PDF buttons on each sheet: **Sheet PDF** (WotC fillable export via pdf-lib) + **Print** (browser print → save-as-PDF of the live rendered sheet).

### dnd file map

```
dnd/
  index.html             browse page + upload panel
  index.js               browse search/sort/filter + upload form logic
  sheet.js               sheet engine (renders DOM from character JSON + localStorage state)
  sheet.css              sheet styles (incl. @media print)
  upload.js              browser-side PDF parser (pdf.js)
  pdf-export.js          WotC PDF fillable export (pdf-lib, lazy-loaded)
  class-resources.js     class+level → resources + spell slots lookup
  POLISH-PROCESS.md      Wren's 15-step procedure for polishing PRs
  assets/5E_CharacterSheet_Fillable.pdf
  characters/*.json      one per character
  characters/index.json  generated summary (regen by scripts/build-dnd-index.js)
  grosh.html             frozen relic (do not modify)
  <slug>.html            thin shims auto-generated by build script
api/
  upload.js              Vercel function: new character PR
  update.js              Vercel function: update existing character PR
  README.md              GITHUB_TOKEN setup (env var name: dnd_upload)
scripts/build-dnd-index.js     regen index.json + per-character shims
.github/workflows/build-dnd-index.yml  runs build script on PR merge, race-safe retry
```

### dnd wiring + services

- **Vercel project**: `dnd-upload` (team `maltysnacks-projects`). Production URL `dnd-upload-maltysnacks-projects.vercel.app`. Auto-deploy from GitHub not connected; redeploy manually via `vercel deploy --prod --yes` from repo root after touching `api/`.
- **GitHub PAT** env var on Vercel: stored as `dnd_upload` (not `GITHUB_TOKEN`). Function reads any of `GITHUB_TOKEN | dnd_upload | DND_UPLOAD | GH_TOKEN`.
- **Build action**: triggers on push to main with paths `dnd/characters/*.json` (excl. index.json + README.md). Retries push 3x on race rejection.

### dnd decisions worth not re-litigating

- **Per-character HTML filename = slug-word1-word2.html** (not query-string routing).
- **No backend for the static site**. Cross-device state sync deferred.
- **PR-based human review** chosen over auto-merge. The merge click is the spam protection.
- **Wren = the AI's name on this repo.** Co-authored-by `Wren <wren@maltysnack.dev>`.
- **Pip vs counter rule**: max ≤ 8 → pip, max > 8 → counter. Coded in `class-resources.js` via `dieType()`.
- **Wiki links**: Google search restricted to `dnd5e.wikidot.com` for non-spell items. Direct deep-link for spells. Homebrew detected by word "homebrew"/"hb" → no link.
- **Feature grouping**: optional `group` field on features. Consecutive same-group features cluster under a wiki-linked header.

### dnd schema cheat sheet

```json
{
  "schemaVersion": 1, "id": "slug-w1-w2", "descriptors": ["w1","w2"],
  "identity": { "name","classLine","background","speedLine","languages","otherProficiencies" },
  "class","level","profBonus","spellcastingAbility",
  "combat": { "ac","hpMax","hitDiceMax","hitDiceType" },
  "abilities":[{key,name,score}x6], "saves":[{ability,proficient}x6], "skills":[x18],
  "attacks":[{name,atk,notes,extra}], "reaction":{title,desc},
  "spellSlots":{"1":4}, "spellSlotsRestoresOn":"short",
  "resources":[{key,name,type:pip|counter|toggle,max,restoresOn}],
  "spellSource":"includes ... oath spells",
  "spells":[{lvl,lvlLabel,name,slug,tags,desc}],
  "limitedSpells":[{group,key,name,slug,count,restoresOn,tags,meta,desc}],
  "racialTraits":[{name,desc}],
  "features":[{name,group,tag,desc,upcoming}],
  "defaultInventory":[{name,notes}]
}
```

## In flight

- **Magic meta-explorer Archetypes tab (LOCAL ONLY, do not push).** New 4th tab on `/games/magic.html`. Uses NMF clustering of recent decks to discover card packages.
  - Uncommitted files: `games/magic.js`, `games/magic.css` (tab + UI), `games/data/cluster.py` (new, NMF script), `games/data/clusters.json` (new, 16 clusters, 87% archetype-classification).
  - Local server to view: `python3 -m http.server 8765 --bind 127.0.0.1` from repo root, then `http://127.0.0.1:8765/games/magic.html#t=archetypes`.
  - Closed cluster card = 2x2 thumbnail preview of top 4 cards.
  - Open cluster card = n×n grid spans at three discrete sizes (anchor 6×6, medium 4×4, small 2×2) on col-width 22px × row-height 30px (card aspect 0.72), dense flow, 560px max-width centered. Outer `.cluster-grid` uses `grid-auto-flow: row dense` so siblings reflow when one opens full-width.
  - Lands filtered from clustering input (they cluster by color, not strategy).
  - Search on the tab filters lists by name; cluster also matches when a using-archetype name matches.
  - "Used by archetypes" and "Travels with" sections removed from cluster open view per user.
  - Selected cards from other tabs get red outline in cluster tiles.

## Decisions pending

- **Cluster overlap dedupe.** NMF gives c1 (MG Landfall) and c13 (MG Landfall + Erode) as near-duplicates. Should post-process in `cluster.py` to merge or label as variant.
- **In-place card panel on Archetypes.** Clicking a tile should not jump to Cards tab. Open a card-cluster panel inline.
- **2D force-directed graph view** as sub-toggle in Archetypes (D3). Benched per user, keep in mind.
- **Travels-with mini graph lines** inside cluster cards (SVG, opacity = co-occurrence).
- **Story signal detector in cron** + Tuesday GitHub-issue auto-open. Before next data refresh.
- **dnd: Vercel GitHub auto-deploy** for `api/` on push (currently manual `vercel deploy`).
- **dnd: cloud-keyed session state** when multi-device becomes a real workflow.

## Gotchas

- The browser FPL formula in `projects/fpl/index.html` is duplicated in `projects/fpl/sync.js`. They must stay aligned.
- A handful of past commits show maltysnack's profile display name as the author rather than `maltysnack`. Same email, maps to the same account on GitHub.
- Vercel functions in `api/upload.js` and `api/update.js` need an explicit `committer`/`author` payload to commit as `maltysnack` rather than the PAT owner's profile. Already done; if removed, upload PRs revert to showing the PAT owner's display name.
- The two flox docs pages and `projects/fpl/index.html` use `data-layout="full"`. Don't accidentally drop the attribute.
- **pdf-lib `setText()` does NOT regenerate appearance streams** on existing form fields. Always call `form.updateFieldAppearances()` before save, or AC/Init/Speed render the cached appearance instead of the new value.
- **dnd parser had a Race-dropping bug** (the `Race ` field with trailing space was in the `handled` set but never read into the JSON). Fixed 08-05-2026. Pre-fix uploads had race silently lost; Jhotan + Japlan were restored manually.

## Things that would surprise the next session

- `dnd/.claude/launch.json` is occasionally untracked and re-appears as a warning in sanitize. Safe to ignore.
- Force-pushed history rewrites happen when consolidating identity. If your local is stale after one, `git fetch && git reset --hard origin/main`.
- Multiple Claude sessions push in parallel. Pull-rebase before non-trivial edits to shared files (`sidebar.js`, `index.html`, `style.css`, `CLAUDE.md`, `HANDOFF.md`, `README.md`).
- **Case-insensitive macOS filesystem will silently merge `handoff.md` and `HANDOFF.md`.** Always use uppercase. If you `Write` lowercase, you overwrite the existing one.
- **Co-author trailer should be `Co-Authored-By: Wren <wren@maltysnack.dev>`**, not Claude. CLAUDE.md spells this out.
- dnd live sheet state is localStorage per-browser-per-device. Switching devices = manual Export/Import (buttons removed but flow could be added back).
